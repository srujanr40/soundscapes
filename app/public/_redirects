# Redirect all client-side routes to index.html for SPA behavior
/* /index.html 200

# Serve all static files directly from the appropriate directories
/_next/static/*  /app/.next/static/:splat  200
/_next/image/*   /app/.next/image/:splat   200
/_next/data/*    /app/.next/data/:splat    200

# Serve assets from the public directory directly
/images/*        /app/public/images/:splat 200
/static/*        /app/public/static/:splat 200
/fonts/*         /app/public/fonts/:splat  200
/audio/*         /app/public/audio/:splat  200
/css/*           /app/public/css/:splat    200
/js/*            /app/public/js/:splat     200

# Serve API routes correctly if used
/api/*           /app/.next/server/pages/api/:splat 200

# Catch-all for any other routes not matched above
/* /index.html 200