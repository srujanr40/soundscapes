/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['encrypted-tbn0.gstatic.com', 'images.unsplash.com', 'assets.aceternity.com'],
  },
};

export default nextConfig;